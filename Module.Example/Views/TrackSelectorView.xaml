<ContentControl x:Class="Module.Example.Views.TrackSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<ContentControl.Resources>
        <DataTemplate x:Key="TrackDataTemplate" DataType="models:ITrack">
            <TextBlock Text="{Binding Name}" />
        </DataTemplate>
    </ContentControl.Resources>
    <ListView ItemTemplate="{StaticResource TrackDataTemplate}"
              ItemsSource="{Binding Tracks}" />

              SelectedItem="{Binding SelectedTrack}"-->
    <ListBox x:Name="Tracks" Grid.Column="0" SelectedItem="{Binding SelectedTrack}"/>
</ContentControl>
